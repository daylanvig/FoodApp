

<ModelForm 
    Title="@(IsNew ? "Add New Quantity Type" : $"Editing {_quantityType.Type}")"
    IsNew="IsNew"
    OnCancel="Clear"
    OnDelete="Delete"
    OnSave="Save">
    <MudTextField
        Label="Description"
        Placeholder="mL"
        Required
        @bind-Value="_quantityType.Type">
    </MudTextField>
</ModelForm>